{# src/OC/UserBundle/Resources/views/layout.html.twig #}

{# On �tend notre layout #}
{% extends "::layout.html.twig" %}

{% block title %}
  Edit Profile -{{ parent() }}
{% endblock %}

{% block bellowHeader %}
             
{% endblock %}

{% block body %}
    <div class="demand-list" >                                                  
        {{ form_start(editUserForm) }}   
            {{ form_errors(editUserForm) }}
            {{ form_row(editUserForm.name) }}
            <div class="avatar">
                <img src="{{ asset(currentUser.profilImage.webPath("70x70")) }}" alt="{{ currentUser.profilImage.alt }}">
            </div>
            {{ form_row(editUserForm.profilImage) }}
            <img style="width: 50%;" src="{{ asset(currentUser.coverImage.webPath("1500x500")) }}" alt="{{ currentUser.coverImage.alt }}"> 
            {{ form_row(editUserForm.coverImage) }}
            
            {{ form_row(editUserForm.save) }}
        {{ form_end(editUserForm) }}
    </div>
{% endblock %}